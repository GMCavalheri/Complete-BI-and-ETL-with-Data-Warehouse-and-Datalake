# Copy this file to `.env` and adjust if you want non-default credentials.
# docker-compose.yml reads these automatically; `.env` itself is gitignored
# so real credentials (if you ever use non-default ones) never get committed.

POSTGRES_USER=dw_user
POSTGRES_PASSWORD=dw_password
POSTGRES_DB=dw_lake
POSTGRES_PORT=5432
